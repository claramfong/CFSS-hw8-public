---
title: "Extracting Data from NYT Developers Network"
author: "Clara Fong"
date: "`r lubridate::today()`"
output: git_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, include = TRUE)
```

```{r}
# Load Packages
library(tidyverse)
library(stringr)
library(httr)
library(jsonlite)
library(lubridate)

# didn't work to use intall.packages("rtimes") because didn't work with current r version
#so instead downloaded development version from github
#install.packages("devtools")
#devtools::install_github("ropengov/rtimes")
library(rtimes)
```

```{r}
key <- getOption("nyt_key")

base.url <- "https://api.nytimes.com/svc/mostpopular/v2/emailed/{period}.json"
search_term <- "'John Mearsheimer'"

```



